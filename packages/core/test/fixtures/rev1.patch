diff --git a/src/auth.ts b/src/auth.ts
index 1111111..2222222 100644
--- a/src/auth.ts
+++ b/src/auth.ts
@@ -10,6 +10,7 @@ export function login(user: string) {
   const token = makeToken(user);
   if (!token) {
     throw new Error("no token");
+    log("failed");
   }
   return token;
 }
@@ -40,7 +41,9 @@ export function logout(user: string) {
   const token = makeToken(user);
-  audit(user, "logout");
-  cleanup(user);
+  audit(user, "logout", Date.now());
+  cleanup(user);
+  metrics.inc();
+  flush();
   return token;
 }
diff --git a/src/dup.ts b/src/dup.ts
index 3333333..4444444 100644
--- a/src/dup.ts
+++ b/src/dup.ts
@@ -5,6 +5,7 @@ export function first() {
   const x = compute();
+  assert(x);
   return x;
 }
@@ -25,6 +26,7 @@ export function second() {
   const x = compute();
+  assert(x);
   return x;
 }
diff --git a/src/legacy.ts b/src/legacy.ts
deleted file mode 100644
index 5555555..0000000
--- a/src/legacy.ts
+++ /dev/null
@@ -1,3 +0,0 @@
-export function legacy() {
-  return 42;
-}
diff --git a/src/new-feature.ts b/src/new-feature.ts
new file mode 100644
index 0000000..6666666
--- /dev/null
+++ b/src/new-feature.ts
@@ -0,0 +1,4 @@
+export function feature(x: number) {
+  return x * 2;
+}
+export const NAME = "feature";
diff --git a/docs/readme.md b/docs/readme.md
index 7777777..8888888 100644
--- a/docs/readme.md
+++ b/docs/readme.md
@@ -1,3 +1,3 @@
 # Guide

-Old intro line.
+New intro line.
diff --git a/src/old-name.ts b/src/renamed.ts
similarity index 100%
rename from src/old-name.ts
rename to src/renamed.ts
diff --git a/assets/logo.png b/assets/logo.png
index 9999999..aaaaaaa 100644
Binary files a/assets/logo.png and b/assets/logo.png differ
diff --git a/scripts/run.sh b/scripts/run.sh
old mode 100644
new mode 100755
