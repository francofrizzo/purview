diff --git a/src/auth.ts b/src/auth.ts
index 1111111..bbbbbbb 100644
--- a/src/auth.ts
+++ b/src/auth.ts
@@ -12,6 +12,7 @@ export function login(user: string) {
   const token = makeToken(user);
   if (!token) {
     throw new Error("no token");
+    log("failed");
   }
   return token;
 }
@@ -42,7 +43,9 @@ export function logout(user: string) {
   const token = makeToken(user);
-  audit(user, "logout");
-  cleanup(user);
+  audit(user, "logout", clock.now());
+  cleanup(user);
+  metrics.inc();
+  flush();
   return token;
 }
@@ -80,3 +83,6 @@ export function refresh(user: string) {
   const token = makeToken(user);
+  if (isExpired(token)) {
+    return rotate(user);
+  }
   return token;
 }
diff --git a/src/dup.ts b/src/dup.ts
index 3333333..4444444 100644
--- a/src/dup.ts
+++ b/src/dup.ts
@@ -5,6 +5,7 @@ export function first() {
   const x = compute();
+  assert(x);
   return x;
 }
@@ -25,6 +26,7 @@ export function second() {
   const x = compute();
+  assert(x);
   return x;
 }
diff --git a/src/new-feature.ts b/src/new-feature.ts
new file mode 100644
index 0000000..6666666
--- /dev/null
+++ b/src/new-feature.ts
@@ -0,0 +1,4 @@
+export function feature(x: number) {
+  return x * 2;
+}
+export const NAME = "feature";
diff --git a/docs/readme.md b/docs/guide.md
similarity index 91%
rename from docs/readme.md
rename to docs/guide.md
index 7777777..8888888 100644
--- a/docs/readme.md
+++ b/docs/guide.md
@@ -1,3 +1,3 @@
 # Guide

-Old intro line.
+New intro line.
