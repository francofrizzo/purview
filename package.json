{
  "name": "reviewer",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm --filter @reviewer/server dev",
    "start": "node scripts/start.mjs"
  }
}
