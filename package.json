{
  "name": "reviewer",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm --filter @reviewer/server dev",
    "start": "node scripts/start.mjs",
    "smoke:pack": "node scripts/smoke-pack.mjs"
  }
}
